@page "/admin/inventory-history/{ProductId:int}"
@attribute [Authorize(Roles = "Admin, Warehouse")]
@using Microsoft.EntityFrameworkCore
@inject ApplicationDbContext DbContext

<h3>Lịch sử Tồn kho</h3>

<!-- FORM TÌM KIẾM -->
<div class="row mb-3">
    <div class="col-md-3">
        <input @bind="searchReason" class="form-control" placeholder="Nhập lý do thay đổi..." />
    </div>
    <div class="col-md-3">
        <InputDate @bind-Value="fromDate" class="form-control" placeholder="Từ ngày" />
    </div>
    <div class="col-md-3">
        <InputDate @bind-Value="toDate" class="form-control" placeholder="Đến ngày" />
    </div>
    <div class="col-md-3">
        <button class="btn btn-primary me-2" @onclick="SearchLogs"><i class="bi bi-search"></i> Tìm kiếm</button>
        <button class="btn btn-secondary" @onclick="ClearSearch"><i class="bi bi-x-circle"></i> Xóa lọc</button>
    </div>
</div>

@if (product == null)
{
    <LoadingSpinner />
}
else
{
    <h5 class="mt-3">Sản phẩm: @product.Name</h5>

    <table class="table table-striped mt-3">
        <thead>
            <tr>
                <th>Thời gian</th>
                <th>Lý do</th>
                <th class="text-end">Thay đổi</th>
                <th class="text-end">Tồn kho mới</th>
            </tr>
        </thead>
        <tbody>
            @if (pagedLogs != null && pagedLogs.Any())
            {
                @foreach (var log in pagedLogs)
                {
                    <tr>
                        <td>@log.Timestamp.ToString("dd/MM/yyyy HH:mm")</td>
                        <td>@log.Reason</td>
                        <td class="text-end @(log.QuantityChange >= 0 ? "text-success" : "text-danger")">
                            <strong>@log.QuantityChange.ToString("+#;-#;0")</strong>
                        </td>
                        <td class="text-end">@log.NewQuantity</td>
                    </tr>
                }
            }
            else
            {
                <tr><td colspan="4" class="text-center text-muted">Không có dữ liệu phù hợp</td></tr>
            }
        </tbody>
    </table>

    <!-- PHÂN TRANG -->
    @if (totalPages > 1)
    {
        <nav>
            <ul class="pagination justify-content-center">
                <li class="page-item @(currentPage == 1 ? "disabled" : "")">
                    <button class="page-link" @onclick="() => ChangePage(currentPage - 1)">Trước</button>
                </li>

                @for (int i = 1; i <= totalPages; i++)
                {
                    <li class="page-item @(currentPage == i ? "active" : "")">
                        <button class="page-link" @onclick="() => ChangePage(i)">@i</button>
                    </li>
                }

                <li class="page-item @(currentPage == totalPages ? "disabled" : "")">
                    <button class="page-link" @onclick="() => ChangePage(currentPage + 1)">Sau</button>
                </li>
            </ul>
        </nav>
    }

    <a href="/admin/products" class="btn btn-secondary mt-3">Quay lại</a>
}

@code {
    [Parameter]
    public int ProductId { get; set; }

    private Product? product;
    private List<InventoryLog>? logs;
    private IEnumerable<InventoryLog>? pagedLogs;

    // Bộ lọc tìm kiếm
    private string? searchReason;
    private DateTime? fromDate;
    private DateTime? toDate;

    // Phân trang
    private int currentPage = 1;
    private int pageSize = 10;
    private int totalPages = 1;

    protected override async Task OnInitializedAsync()
    {
        product = await DbContext.Products.FindAsync(ProductId);
        if (product != null)
        {
            await LoadLogs();
        }
    }

    private async Task LoadLogs()
    {
        var query = DbContext.InventoryLogs
            .Where(l => l.ProductId == ProductId)
            .AsQueryable();

        if (!string.IsNullOrWhiteSpace(searchReason))
        {
            query = query.Where(l => l.Reason.Contains(searchReason));
        }

        if (fromDate.HasValue)
        {
            query = query.Where(l => l.Timestamp.Date >= fromDate.Value.Date);
        }

        if (toDate.HasValue)
        {
            query = query.Where(l => l.Timestamp.Date <= toDate.Value.Date);
        }

        query = query.OrderByDescending(l => l.Timestamp);

        logs = await query.ToListAsync();

        // Phân trang
        totalPages = (int)Math.Ceiling((double)logs.Count / pageSize);
        currentPage = Math.Clamp(currentPage, 1, totalPages == 0 ? 1 : totalPages);

        pagedLogs = logs.Skip((currentPage - 1) * pageSize).Take(pageSize);
    }

    private async Task SearchLogs()
    {
        currentPage = 1;
        await LoadLogs();
    }

    private async Task ClearSearch()
    {
        searchReason = null;
        fromDate = null;
        toDate = null;
        currentPage = 1;
        await LoadLogs();
    }

    private async Task ChangePage(int page)
    {
        currentPage = page;
        await LoadLogs();
    }
}
